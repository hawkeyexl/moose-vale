# Dogfood: this repo's own prose is checked with the package it ships.
StylesPath = .vale/styles
MinAlertLevel = suggestion
Packages = ./Moose

# The fixtures are deliberately bad. They are test input, not prose.
[fixtures/**]
BasedOnStyles =
